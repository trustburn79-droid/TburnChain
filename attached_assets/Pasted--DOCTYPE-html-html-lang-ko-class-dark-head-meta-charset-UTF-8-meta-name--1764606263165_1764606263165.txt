<!DOCTYPE html>
<html lang="ko" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Node | TBurn Chain</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Space Grotesk', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        cyber: {
                            black: '#050505',
                            dark: '#0a0a0f',
                            cyan: '#00f0ff',
                            purple: '#7000ff',
                            glass: 'rgba(255, 255, 255, 0.03)',
                            border: 'rgba(255, 255, 255, 0.08)'
                        }
                    },
                    animation: {
                        'spin-slow': 'spin 8s linear infinite',
                    }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #050505;
            color: #e2e8f0;
            overflow-x: hidden;
        }

        /* 1. Background */
        #neural-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: -1;
            opacity: 0.5;
            pointer-events: none;
        }

        /* 2. Spotlight Card */
        .spotlight-card {
            background-color: rgba(20, 20, 25, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
            position: relative;
            overflow: hidden;
            transition: transform 0.3s ease;
        }
        
        .spotlight-card::before {
            content: "";
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            border-radius: inherit;
            padding: 1.5px;
            background: radial-gradient(
                800px circle at var(--mouse-x) var(--mouse-y),
                rgba(0, 240, 255, 0.5),
                transparent 40%
            );
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.5s;
            pointer-events: none;
        }

        .spotlight-card:hover::before { opacity: 1; }

        /* 3. Input Styles */
        .cyber-input {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            transition: all 0.3s;
        }
        .cyber-input:focus {
            border-color: #00f0ff;
            box-shadow: 0 0 10px rgba(0, 240, 255, 0.2);
            outline: none;
        }

        /* 4. Button Glow */
        .btn-neon {
            background: linear-gradient(90deg, #00f0ff, #0099ff);
            color: black;
            font-weight: bold;
            position: relative;
            overflow: hidden;
            transition: all 0.3s;
        }
        .btn-neon:hover {
            box-shadow: 0 0 20px rgba(0, 240, 255, 0.4);
            transform: translateY(-1px);
        }
    </style>
</head>
<body class="antialiased flex flex-col min-h-screen">

    <canvas id="neural-canvas"></canvas>

    <nav class="sticky top-0 z-50 w-full border-b border-white/5 bg-black/60 backdrop-blur-md">
        <div class="max-w-7xl mx-auto px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
                <a href="/" class="flex items-center gap-2 group">
                    <div class="relative w-8 h-8 rounded bg-gradient-to-br from-cyber-cyan to-cyber-purple flex items-center justify-center group-hover:scale-110 transition-transform">
                        <i class="fas fa-fire text-black text-sm"></i>
                        <div class="absolute inset-0 bg-white/20 rounded blur opacity-0 group-hover:opacity-100 transition-opacity"></div>
                    </div>
                    <span class="font-bold text-xl text-white tracking-tight">TBurn <span class="text-cyber-cyan">Chain</span></span>
                </a>
                
                <div class="hidden md:flex items-center gap-6">
                    <a href="#" class="text-sm font-medium text-gray-400 hover:text-white transition">Developers</a>
                    <a href="#" class="text-sm font-medium text-gray-400 hover:text-white transition">Network</a>
                    <button class="text-gray-400 hover:text-cyber-cyan transition">
                        <i class="fas fa-globe"></i>
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <main class="flex-grow flex items-center justify-center p-4 relative z-10" id="login-container">
        
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[400px] bg-cyber-purple/20 blur-[100px] rounded-full pointer-events-none"></div>

        <div class="w-full max-w-4xl grid md:grid-cols-2 gap-6">
            
            <div class="spotlight-card rounded-2xl p-8 flex flex-col justify-center">
                <div class="mb-8">
                    <div class="inline-flex items-center gap-2 px-2 py-1 rounded bg-cyber-cyan/10 border border-cyber-cyan/20 text-cyber-cyan text-xs font-mono mb-4">
                        <span class="w-1.5 h-1.5 rounded-full bg-cyber-cyan animate-pulse"></span>
                        GATEWAY_SECURE
                    </div>
                    <h1 class="text-3xl font-bold text-white mb-2 scramble-target" data-value="Authenticate">Loading...</h1>
                    <p class="text-sm text-gray-400">Connect to the Neural Consensus Layer</p>
                </div>

                <div class="space-y-4">
                    <button class="btn-neon w-full py-3 rounded-lg flex items-center justify-center gap-3">
                        <i class="fas fa-wallet"></i>
                        <span>Connect Wallet</span>
                    </button>
                    
                    <div class="relative py-2">
                        <div class="absolute inset-0 flex items-center"><span class="w-full border-t border-white/10"></span></div>
                        <div class="relative flex justify-center text-xs uppercase"><span class="bg-[#121216] px-2 text-gray-500">Or via Replit Auth</span></div>
                    </div>

                    <div class="grid grid-cols-2 gap-3">
                        <button class="cyber-input py-2.5 rounded-lg text-sm hover:bg-white/5 transition flex items-center justify-center gap-2">
                            <i class="fab fa-google"></i> Google
                        </button>
                        <button class="cyber-input py-2.5 rounded-lg text-sm hover:bg-white/5 transition flex items-center justify-center gap-2">
                            <i class="fab fa-github"></i> GitHub
                        </button>
                    </div>
                </div>

                <p class="text-xs text-gray-600 text-center mt-6">
                    By connecting, you agree to the <a href="#" class="text-cyber-cyan hover:underline">Protocol Protocols</a>.
                </p>
            </div>

            <div class="spotlight-card rounded-2xl p-8 border-cyber-cyan/30 flex flex-col justify-center relative overflow-hidden group">
                <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-cyber-cyan/50 to-transparent opacity-50 animate-pulse"></div>

                <div class="mb-6 relative z-10">
                    <h2 class="text-2xl font-bold text-white mb-2 flex items-center gap-2">
                        <i class="fas fa-cube text-cyber-purple"></i> Why Connect?
                    </h2>
                    <p class="text-sm text-gray-400">Join the world's first trust-based Layer 1.</p>
                </div>

                <ul class="space-y-4 relative z-10">
                    <li class="flex items-start gap-3 group/item">
                        <div class="mt-1 w-5 h-5 rounded-full bg-cyber-cyan/10 flex items-center justify-center border border-cyber-cyan/30 group-hover/item:border-cyber-cyan transition">
                            <i class="fas fa-check text-[10px] text-cyber-cyan"></i>
                        </div>
                        <div>
                            <span class="block text-sm text-white font-medium">Testnet Incentives</span>
                            <span class="text-xs text-gray-500">Early participants receive genesis multipliers.</span>
                        </div>
                    </li>
                    <li class="flex items-start gap-3 group/item">
                        <div class="mt-1 w-5 h-5 rounded-full bg-cyber-cyan/10 flex items-center justify-center border border-cyber-cyan/30 group-hover/item:border-cyber-cyan transition">
                            <i class="fas fa-code-branch text-[10px] text-cyber-cyan"></i>
                        </div>
                        <div>
                            <span class="block text-sm text-white font-medium">Governance Rights</span>
                            <span class="text-xs text-gray-500">Shape the mainnet policy development.</span>
                        </div>
                    </li>
                    <li class="flex items-start gap-3 group/item">
                        <div class="mt-1 w-5 h-5 rounded-full bg-cyber-cyan/10 flex items-center justify-center border border-cyber-cyan/30 group-hover/item:border-cyber-cyan transition">
                            <i class="fas fa-book text-[10px] text-cyber-cyan"></i>
                        </div>
                        <div>
                            <span class="block text-sm text-white font-medium">Developer Docs</span>
                            <span class="text-xs text-gray-500">Access exclusive technical whitepapers.</span>
                        </div>
                    </li>
                </ul>

                <i class="fas fa-network-wired absolute -bottom-10 -right-10 text-9xl text-white/5 rotate-12 group-hover:rotate-0 transition-transform duration-700"></i>
            </div>
        </div>
    </main>

    <footer class="border-t border-white/5 bg-black/40 backdrop-blur-sm mt-auto">
        <div class="max-w-7xl mx-auto px-6 py-8">
            <div class="flex flex-col md:flex-row justify-between items-center gap-4">
                <div class="text-center md:text-left">
                    <p class="text-sm text-gray-500">© 2025 TBurn Chain Foundation. <span class="text-cyber-cyan/50">All systems operational.</span></p>
                </div>
                <div class="flex gap-6 text-gray-500 text-sm">
                    <a href="#" class="hover:text-cyber-cyan transition">Privacy</a>
                    <a href="#" class="hover:text-cyber-cyan transition">Terms</a>
                    <a href="#" class="hover:text-cyber-cyan transition">Whitepaper</a>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // 1. Text Scramble Animation
        const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
        document.querySelectorAll(".scramble-target").forEach(target => {
            let iterations = 0;
            const finalValue = target.dataset.value;
            const interval = setInterval(() => {
                target.innerText = finalValue.split("")
                    .map((letter, index) => {
                        if(index < iterations) return finalValue[index];
                        return letters[Math.floor(Math.random() * letters.length)];
                    })
                    .join("");
                if(iterations >= finalValue.length) clearInterval(interval);
                iterations += 1/3;
            }, 40);
        });

        // 2. Spotlight Effect Logic
        const container = document.getElementById("login-container");
        const cards = document.querySelectorAll(".spotlight-card");
        
        container.addEventListener("mousemove", (e) => {
            cards.forEach(card => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                card.style.setProperty("--mouse-x", `${x}px`);
                card.style.setProperty("--mouse-y", `${y}px`);
            });
        });

        // 3. Background Particles
        const canvas = document.getElementById('neural-canvas');
        const ctx = canvas.getContext('2d');
        let particlesArray;
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        class Particle {
            constructor(x, y, directionX, directionY, size) {
                this.x = x; this.y = y;
                this.directionX = directionX; this.directionY = directionY;
                this.size = size;
            }
            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2, false);
                ctx.fillStyle = '#00f0ff'; // Cyan particles
                ctx.fill();
            }
            update() {
                if (this.x > canvas.width || this.x < 0) this.directionX = -this.directionX;
                if (this.y > canvas.height || this.y < 0) this.directionY = -this.directionY;
                this.x += this.directionX;
                this.y += this.directionY;
                this.draw();
            }
        }

        function init() {
            particlesArray = [];
            let numberOfParticles = (canvas.height * canvas.width) / 18000; // Less dense for login
            for (let i = 0; i < numberOfParticles; i++) {
                let size = (Math.random() * 2) + 0.1;
                let x = Math.random() * canvas.width;
                let y = Math.random() * canvas.height;
                let directionX = (Math.random() * 0.2) - 0.1; // Slower movement
                let directionY = (Math.random() * 0.2) - 0.1;
                particlesArray.push(new Particle(x, y, directionX, directionY, size));
            }
        }

        function animate() {
            requestAnimationFrame(animate);
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            for (let i = 0; i < particlesArray.length; i++) {
                particlesArray[i].update();
            }
            connect();
        }

        function connect() {
            for (let a = 0; a < particlesArray.length; a++) {
                for (let b = a; b < particlesArray.length; b++) {
                    let distance = ((particlesArray[a].x - particlesArray[b].x) * (particlesArray[a].x - particlesArray[b].x)) + 
                                   ((particlesArray[a].y - particlesArray[b].y) * (particlesArray[a].y - particlesArray[b].y));
                    if (distance < (canvas.width/7) * (canvas.height/7)) {
                        let opacityValue = 1 - (distance/20000);
                        ctx.strokeStyle = 'rgba(0, 240, 255,' + opacityValue * 0.1 + ')'; // Faint cyan lines
                        ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.moveTo(particlesArray[a].x, particlesArray[a].y);
                        ctx.lineTo(particlesArray[b].x, particlesArray[b].y);
                        ctx.stroke();
                    }
                }
            }
        }

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            init();
        });

        init();
        animate();
    </script>
</body>
</html>
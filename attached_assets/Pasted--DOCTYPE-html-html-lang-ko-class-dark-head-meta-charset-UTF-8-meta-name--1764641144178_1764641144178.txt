<!DOCTYPE html>
<html lang="ko" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In | TBurn Chain Membership</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Space Grotesk', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        brand: {
                            bg: '#030407',
                            cyan: '#00f0ff',
                            purple: '#7000ff',
                            border: 'rgba(255, 255, 255, 0.08)',
                        }
                    },
                    animation: {
                        'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                    }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #030407;
            color: #ffffff;
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Background Canvas */
        #neural-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: -1;
            opacity: 0.25;
        }

        /* Glassmorphism */
        .glass-panel {
            background: rgba(20, 20, 25, 0.6); 
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2);
        }
        
        .glass-nav {
            background: rgba(3, 4, 7, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        /* Spotlight Effect */
        .spotlight-card {
            background-color: rgba(15, 15, 20, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.08);
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
        }
        
        .spotlight-card::before {
            content: "";
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            border-radius: inherit;
            padding: 1.5px;
            background: radial-gradient(
                600px circle at var(--mouse-x) var(--mouse-y),
                rgba(112, 0, 255, 0.4),
                transparent 40%
            );
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.5s;
            pointer-events: none;
        }

        .spotlight-card:hover::before { opacity: 1; }
        .spotlight-card:hover { 
            transform: translateY(-2px);
            border-color: rgba(112, 0, 255, 0.3);
            box-shadow: 0 10px 30px -10px rgba(112, 0, 255, 0.1);
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #030407; }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }
        
        main { flex: 1; }
    </style>
</head>
<body class="antialiased selection:bg-brand-purple/30 selection:text-white">

    <canvas id="neural-canvas"></canvas>

    <nav class="fixed top-0 w-full z-50 glass-nav">
        <div class="max-w-7xl mx-auto px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
                <a href="/" class="flex items-center gap-3 cursor-pointer group">
                    <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-brand-purple to-brand-cyan flex items-center justify-center shadow-[0_0_15px_rgba(112,0,255,0.4)]">
                        <span class="text-white font-bold text-lg font-sans">B</span>
                    </div>
                    <span class="font-bold text-lg tracking-tight text-white">TBurn <span class="text-brand-purple font-light">ID</span></span>
                </a>
                <div class="flex items-center gap-4">
                    <a href="/" class="text-sm font-medium text-gray-400 hover:text-white transition-colors">Back to Home</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="relative flex items-center justify-center pt-20 pb-20 px-4">
        <div class="absolute inset-0 bg-gradient-to-b from-brand-purple/10 via-transparent to-transparent pointer-events-none"></div>
        
        <div class="w-full max-w-5xl grid md:grid-cols-2 gap-8 relative z-10">
            
            <div class="spotlight-card rounded-2xl p-8 flex flex-col justify-center h-full border border-brand-purple/20">
                <div class="mb-8 text-center">
                    <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-brand-purple to-brand-cyan flex items-center justify-center mx-auto mb-6 shadow-lg">
                        <i class="fas fa-fingerprint text-3xl text-white"></i>
                    </div>
                    <h1 class="text-3xl font-bold text-white mb-2">Welcome Back</h1>
                    <p class="text-gray-400">Access your dashboard and rewards</p>
                </div>

                <div class="space-y-4">
                    <button class="w-full bg-brand-purple hover:bg-purple-600 text-white font-bold py-3 px-4 rounded-lg transition flex items-center justify-center gap-3 shadow-[0_0_20px_rgba(112,0,255,0.3)] group disabled:opacity-70 disabled:cursor-not-allowed" disabled>
                        <span class="w-2 h-2 rounded-full bg-white/50 group-hover:bg-white animate-pulse"></span>
                        Testnet Participation (Preparing)
                    </button>
                    
                    <div class="relative py-4">
                        <div class="absolute inset-0 flex items-center">
                            <span class="w-full border-t border-gray-800"></span>
                        </div>
                        <div class="relative flex justify-center text-xs uppercase">
                            <span class="bg-[#13131a] px-2 text-gray-500">Authentication Provider</span>
                        </div>
                    </div>

                    <button class="w-full bg-white/5 hover:bg-white/10 border border-white/10 text-white py-3 px-4 rounded-lg transition flex items-center justify-center gap-3">
                        <i class="fab fa-google text-white"></i> Continue with Google
                    </button>
                    <button class="w-full bg-white/5 hover:bg-white/10 border border-white/10 text-white py-3 px-4 rounded-lg transition flex items-center justify-center gap-3">
                        <i class="fab fa-github text-white"></i> Continue with GitHub
                    </button>
                </div>

                <p class="text-xs text-center text-gray-500 mt-6">
                    By signing in, you agree to our <a href="#" class="text-brand-purple hover:underline">Terms</a> and <a href="#" class="text-brand-purple hover:underline">Privacy Policy</a>.
                </p>
            </div>

            <div class="glass-panel rounded-2xl p-8 flex flex-col justify-center h-full border border-brand-cyan/20 relative overflow-hidden">
                <div class="absolute top-0 right-0 w-64 h-64 bg-brand-cyan/10 rounded-full blur-[60px] pointer-events-none"></div>
                
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-2">
                    <i class="fas fa-star text-brand-cyan"></i> Member Benefits
                </h2>

                <ul class="space-y-6">
                    <li class="flex items-start gap-4">
                        <div class="w-10 h-10 rounded-full bg-brand-cyan/10 flex items-center justify-center text-brand-cyan flex-shrink-0 mt-1">
                            <i class="fas fa-flask"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-white">Testnet Early Access</h3>
                            <p class="text-sm text-gray-400">Be the first to test new features and earn contribution points.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-10 h-10 rounded-full bg-brand-purple/10 flex items-center justify-center text-brand-purple flex-shrink-0 mt-1">
                            <i class="fas fa-comments"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-white">Governance Participation</h3>
                            <p class="text-sm text-gray-400">Influence the future of TBurn Chain through proposal voting.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-10 h-10 rounded-full bg-pink-500/10 flex items-center justify-center text-pink-500 flex-shrink-0 mt-1">
                            <i class="fas fa-book"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-white">Technical Resources</h3>
                            <p class="text-sm text-gray-400">Full access to developer documentation, SDKs, and API keys.</p>
                        </div>
                    </li>
                    <li class="flex items-start gap-4">
                        <div class="w-10 h-10 rounded-full bg-yellow-500/10 flex items-center justify-center text-yellow-500 flex-shrink-0 mt-1">
                            <i class="fas fa-gift"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-white">Ecosystem Rewards</h3>
                            <p class="text-sm text-gray-400">Exclusive airdrops and rewards for active community members.</p>
                        </div>
                    </li>
                </ul>
            </div>

        </div>
    </main>

    <footer class="border-t border-brand-border bg-black/50 backdrop-blur-xl mt-auto">
        <div class="max-w-7xl mx-auto px-6 lg:px-8 py-8 text-center">
            <p class="text-gray-600 text-sm">© 2025 TBurn Chain Foundation. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // 1. Neural Canvas (Purple/Cyan Theme)
        const canvas = document.getElementById('neural-canvas');
        const ctx = canvas.getContext('2d');
        let particlesArray;

        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const colors = ['#7000ff', '#00f0ff'];

        class Particle {
            constructor(x, y, directionX, directionY, size, color) {
                this.x = x; this.y = y;
                this.directionX = directionX; this.directionY = directionY;
                this.size = size;
                this.color = color;
            }
            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2, false);
                ctx.fillStyle = this.color;
                ctx.fill();
            }
            update() {
                if (this.x > canvas.width || this.x < 0) this.directionX = -this.directionX;
                if (this.y > canvas.height || this.y < 0) this.directionY = -this.directionY;
                this.x += this.directionX;
                this.y += this.directionY;
                this.draw();
            }
        }

        function init() {
            particlesArray = [];
            let numberOfParticles = (canvas.height * canvas.width) / 25000;
            for (let i = 0; i < numberOfParticles; i++) {
                let size = (Math.random() * 1.5) + 0.5;
                let x = (Math.random() * innerWidth);
                let y = (Math.random() * innerHeight);
                let directionX = (Math.random() * 0.1) - 0.05;
                let directionY = (Math.random() * 0.1) - 0.05;
                let color = colors[Math.floor(Math.random() * colors.length)];
                particlesArray.push(new Particle(x, y, directionX, directionY, size, color));
            }
        }

        function animate() {
            requestAnimationFrame(animate);
            ctx.clearRect(0, 0, innerWidth, innerHeight);
            
            for (let i = 0; i < particlesArray.length; i++) {
                particlesArray[i].update();
            }
            connect();
        }

        function connect() {
            for (let a = 0; a < particlesArray.length; a++) {
                for (let b = a; b < particlesArray.length; b++) {
                    let distance = ((particlesArray[a].x - particlesArray[b].x) * (particlesArray[a].x - particlesArray[b].x)) + 
                                   ((particlesArray[a].y - particlesArray[b].y) * (particlesArray[a].y - particlesArray[b].y));
                    if (distance < (canvas.width/7) * (canvas.height/7)) {
                        let opacityValue = 1 - (distance/20000);
                        ctx.strokeStyle = 'rgba(112, 0, 255,' + opacityValue * 0.1 + ')'; // Purple connection
                        ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.moveTo(particlesArray[a].x, particlesArray[a].y);
                        ctx.lineTo(particlesArray[b].x, particlesArray[b].y);
                        ctx.stroke();
                    }
                }
            }
        }

        window.addEventListener('resize', () => {
            canvas.width = innerWidth;
            canvas.height = innerHeight;
            init();
        });
        init();
        animate();

        // 2. Spotlight Interaction
        const cards = document.querySelectorAll(".spotlight-card");
        document.addEventListener("mousemove", (e) => {
            cards.forEach(card => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                card.style.setProperty("--mouse-x", `${x}px`);
                card.style.setProperty("--mouse-y", `${y}px`);
            });
        });
    </script>
</body>
</html>
<!DOCTYPE html>
<html lang="ko" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard | TBurn Chain</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Space Grotesk', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        brand: {
                            bg: '#030407',
                            cyan: '#00f0ff',
                            purple: '#7000ff',
                            border: 'rgba(255, 255, 255, 0.08)',
                            surface: '#0a0a0f',
                        }
                    },
                    animation: {
                        'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                    }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #030407;
            color: #ffffff;
            overflow-x: hidden;
        }

        /* Background Canvas */
        #neural-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: -1;
            opacity: 0.15;
        }

        /* Glassmorphism */
        .glass-panel {
            background: rgba(20, 20, 25, 0.6); 
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2);
        }
        
        .glass-nav {
            background: rgba(3, 4, 7, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        /* Spotlight Effect */
        .spotlight-card {
            background-color: rgba(15, 15, 20, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.08);
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
        }
        
        .spotlight-card::before {
            content: "";
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            border-radius: inherit;
            padding: 1.5px;
            background: radial-gradient(
                600px circle at var(--mouse-x) var(--mouse-y),
                rgba(0, 240, 255, 0.3),
                transparent 40%
            );
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.5s;
            pointer-events: none;
        }

        .spotlight-card:hover::before { opacity: 1; }
        .spotlight-card:hover { 
            transform: translateY(-2px);
            border-color: rgba(0, 240, 255, 0.2);
            box-shadow: 0 5px 15px -5px rgba(0, 240, 255, 0.1);
        }

        /* Tab Styles */
        .tab-active {
            background: rgba(112, 0, 255, 0.2);
            color: #ffffff;
            border-bottom: 2px solid #7000ff;
        }
        .tab-inactive {
            color: #9ca3af;
            border-bottom: 2px solid transparent;
        }
        .tab-inactive:hover {
            color: #ffffff;
            background: rgba(255, 255, 255, 0.05);
        }

        /* Scrollbar */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: #030407; }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }
    </style>
</head>
<body class="antialiased selection:bg-brand-purple/30 selection:text-white">

    <canvas id="neural-canvas"></canvas>

    <nav class="fixed top-0 w-full z-50 glass-nav">
        <div class="w-full px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
                <div class="flex items-center gap-3">
                    <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-brand-purple to-brand-cyan flex items-center justify-center shadow-[0_0_10px_rgba(112,0,255,0.3)]">
                        <span class="text-white font-bold text-lg font-sans">B</span>
                    </div>
                    <span class="font-bold text-lg tracking-tight text-white">TBurn <span class="text-brand-purple font-light">Admin</span></span>
                </div>
                
                <div class="hidden md:flex items-center gap-6">
                    <a href="#" class="text-sm font-medium text-gray-400 hover:text-white transition-colors">Dashboard</a>
                    <a href="#" class="text-sm font-medium text-white border-b border-brand-purple pb-1">Content</a>
                    <a href="#" class="text-sm font-medium text-gray-400 hover:text-white transition-colors">Members</a>
                    <a href="#" class="text-sm font-medium text-gray-400 hover:text-white transition-colors">Settings</a>
                </div>

                <div class="flex items-center gap-4">
                    <div class="w-8 h-8 rounded-full bg-gradient-to-br from-gray-700 to-gray-800 flex items-center justify-center border border-white/10 cursor-pointer">
                        <i class="fas fa-user-cog text-xs"></i>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main class="relative pt-24 pb-20 px-6 lg:px-8 max-w-[1200px] mx-auto">
        
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 mb-8">
            <div>
                <h1 class="text-2xl font-bold text-white flex items-center gap-2">
                    <i class="fas fa-edit text-brand-purple"></i> Content Management
                </h1>
                <p class="text-gray-400 text-sm mt-1">Manage blog posts, announcements, and events.</p>
            </div>
        </div>

        <div class="mb-8 border-b border-white/10">
            <div class="flex gap-4">
                <button class="px-6 py-3 text-sm font-medium tab-active transition-all">
                    <i class="fas fa-newspaper mr-2"></i> Blog Posts
                </button>
                <button class="px-6 py-3 text-sm font-medium tab-inactive transition-all">
                    <i class="fas fa-calendar-alt mr-2"></i> Events
                </button>
                <button class="px-6 py-3 text-sm font-medium tab-inactive transition-all">
                    <i class="fas fa-bullhorn mr-2"></i> Announcements
                </button>
            </div>
        </div>

        <div id="blog-content" class="space-y-6">
            
            <div class="flex justify-between items-center">
                <h2 class="text-xl font-bold text-white">Recent Posts</h2>
                <button class="bg-brand-purple hover:bg-purple-600 text-white px-4 py-2 rounded-lg text-sm font-bold transition flex items-center gap-2 shadow-[0_0_15px_rgba(112,0,255,0.3)]">
                    <i class="fas fa-plus"></i> New Post
                </button>
            </div>

            <div class="grid gap-4">
                
                <div class="spotlight-card rounded-xl p-6 flex flex-col md:flex-row gap-6 items-start md:items-center group">
                    <div class="flex-1">
                        <div class="flex items-center gap-3 mb-2">
                            <span class="px-2 py-1 rounded bg-brand-cyan/10 text-brand-cyan text-[10px] font-bold uppercase tracking-wider border border-brand-cyan/20">Network</span>
                            <span class="text-gray-500 text-xs flex items-center gap-1"><i class="far fa-clock"></i> 2025-04-01</span>
                        </div>
                        <h3 class="text-lg font-bold text-white mb-1 group-hover:text-brand-cyan transition">Burn Chain Mainnet Official Launch</h3>
                        <p class="text-sm text-gray-400 line-clamp-1">
                            The world's first trust-based blockchain Burn Chain has launched its mainnet. A next-generation L1 blockchain with 100,000+ TPS performance...
                        </p>
                    </div>
                    <div class="flex gap-2 self-end md:self-center">
                        <button class="p-2 rounded-lg bg-white/5 hover:bg-white/10 text-gray-400 hover:text-white transition" title="Edit">
                            <i class="fas fa-pen"></i>
                        </button>
                        <button class="p-2 rounded-lg bg-white/5 hover:bg-red-500/20 text-gray-400 hover:text-red-500 transition" title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>

                <div class="spotlight-card rounded-xl p-6 flex flex-col md:flex-row gap-6 items-start md:items-center group">
                    <div class="flex-1">
                        <div class="flex items-center gap-3 mb-2">
                            <span class="px-2 py-1 rounded bg-brand-purple/10 text-brand-purple text-[10px] font-bold uppercase tracking-wider border border-brand-purple/20">Technology</span>
                            <span class="text-gray-500 text-xs flex items-center gap-1"><i class="far fa-clock"></i> 2025-03-28</span>
                        </div>
                        <h3 class="text-lg font-bold text-white mb-1 group-hover:text-brand-purple transition">Trust Score System v2.0 Update</h3>
                        <p class="text-sm text-gray-400 line-clamp-1">
                            Trust Score System v2.0 with enhanced AI-based analysis and real-time monitoring has been deployed. More accurate project evaluation...
                        </p>
                    </div>
                    <div class="flex gap-2 self-end md:self-center">
                        <button class="p-2 rounded-lg bg-white/5 hover:bg-white/10 text-gray-400 hover:text-white transition" title="Edit">
                            <i class="fas fa-pen"></i>
                        </button>
                        <button class="p-2 rounded-lg bg-white/5 hover:bg-red-500/20 text-gray-400 hover:text-red-500 transition" title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>

                <div class="spotlight-card rounded-xl p-6 flex flex-col md:flex-row gap-6 items-start md:items-center group border-dashed border-gray-700 bg-black/20">
                    <div class="flex-1">
                        <div class="flex items-center gap-3 mb-2">
                            <span class="px-2 py-1 rounded bg-gray-700/30 text-gray-400 text-[10px] font-bold uppercase tracking-wider border border-gray-600">Draft</span>
                            <span class="text-gray-500 text-xs flex items-center gap-1"><i class="far fa-clock"></i> Last edited 2h ago</span>
                        </div>
                        <h3 class="text-lg font-bold text-gray-300 mb-1">Q2 Ecosystem Roadmap</h3>
                        <p class="text-sm text-gray-500 line-clamp-1">
                            Upcoming features including NFT marketplace integration and cross-chain bridge expansion...
                        </p>
                    </div>
                    <div class="flex gap-2 self-end md:self-center">
                        <button class="p-2 rounded-lg bg-white/5 hover:bg-white/10 text-gray-400 hover:text-white transition" title="Edit">
                            <i class="fas fa-pen"></i>
                        </button>
                        <button class="p-2 rounded-lg bg-white/5 hover:bg-red-500/20 text-gray-400 hover:text-red-500 transition" title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>

            </div>
        </div>

    </main>

    <script>
        // 1. Neural Canvas (Subtle Admin Background)
        const canvas = document.getElementById('neural-canvas');
        const ctx = canvas.getContext('2d');
        let particlesArray;

        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        class Particle {
            constructor(x, y, directionX, directionY, size) {
                this.x = x; this.y = y;
                this.directionX = directionX; this.directionY = directionY;
                this.size = size;
            }
            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2, false);
                ctx.fillStyle = '#7000ff'; // Purple particles for admin
                ctx.fill();
            }
            update() {
                if (this.x > canvas.width || this.x < 0) this.directionX = -this.directionX;
                if (this.y > canvas.height || this.y < 0) this.directionY = -this.directionY;
                this.x += this.directionX;
                this.y += this.directionY;
                this.draw();
            }
        }

        function init() {
            particlesArray = [];
            let numberOfParticles = (canvas.height * canvas.width) / 25000;
            for (let i = 0; i < numberOfParticles; i++) {
                let size = (Math.random() * 1.5) + 0.5;
                let x = (Math.random() * innerWidth);
                let y = (Math.random() * innerHeight);
                let directionX = (Math.random() * 0.1) - 0.05;
                let directionY = (Math.random() * 0.1) - 0.05;
                particlesArray.push(new Particle(x, y, directionX, directionY, size));
            }
        }

        function animate() {
            requestAnimationFrame(animate);
            ctx.clearRect(0, 0, innerWidth, innerHeight);
            
            for (let i = 0; i < particlesArray.length; i++) {
                particlesArray[i].update();
            }
            connect();
        }

        function connect() {
            for (let a = 0; a < particlesArray.length; a++) {
                for (let b = a; b < particlesArray.length; b++) {
                    let distance = ((particlesArray[a].x - particlesArray[b].x) * (particlesArray[a].x - particlesArray[b].x)) + 
                                   ((particlesArray[a].y - particlesArray[b].y) * (particlesArray[a].y - particlesArray[b].y));
                    if (distance < (canvas.width/7) * (canvas.height/7)) {
                        let opacityValue = 1 - (distance/20000);
                        ctx.strokeStyle = 'rgba(112, 0, 255,' + opacityValue * 0.1 + ')';
                        ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.moveTo(particlesArray[a].x, particlesArray[a].y);
                        ctx.lineTo(particlesArray[b].x, particlesArray[b].y);
                        ctx.stroke();
                    }
                }
            }
        }

        window.addEventListener('resize', () => {
            canvas.width = innerWidth;
            canvas.height = innerHeight;
            init();
        });
        init();
        animate();

        // 2. Spotlight Interaction
        const cards = document.querySelectorAll(".spotlight-card");
        document.addEventListener("mousemove", (e) => {
            cards.forEach(card => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                card.style.setProperty("--mouse-x", `${x}px`);
                card.style.setProperty("--mouse-y", `${y}px`);
            });
        });
    </script>
</body>
</html>